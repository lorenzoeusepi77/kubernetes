# RUN PLAYBOOK
#
# Read README.md for detail
#
# ansible-playbook -i kubernetes/inventories/production/hosts kubernetes/site-glusterfs.yml -e clustername=kubernetes -u centos

---
# Main file:
- include: glusterFS.yml
